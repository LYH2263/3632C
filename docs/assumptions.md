# Assumptions

1. MVP 演示以 H5 端验证为主，页面路径按 uniapp 约定组织（`/pages/**`）。
2. 角色权限采用简化策略：买家固定示例账号，商家通过演示账号登录。
3. 默认数据源为本地 mock + storage；API 模式通过运行时配置手动切换。
4. 线下支付仅做展示，不接入线上支付或退款流程。
5. 后端鉴权采用 MVP 轻量 token 返回，不引入完整 JWT/Session 体系。
6. 图片使用占位外链，仅用于演示展示。
